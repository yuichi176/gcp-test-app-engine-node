name: _GET_VERSION_URI

on:
  workflow_dispatch:

jobs:
  generate-version-name:
    name: Generate Version Name
    uses: ./.github/workflows/_generate-version-name.yml
    with:
      BRANCH_NAME: ${{ github.ref_name }}

  get-version-uri:
    runs-on: ubuntu-latest
    needs:
      - generate-version-name
    steps:
      - uses: actions/checkout@v3

      - id: auth
        name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Get GAE Version URI
        id: getGAEVersionURI
        run: |
          VERSION_URI=$(gcloud app versions list --filter="id=${{ needs.generate-version-name.outputs.version_name }}" --uri)
          if [[ "$OUTPUT" == "Listed 0 items." ]]; then
            echo "apiDomain=glb.devchez.com" >> $GITHUB_OUTPUT
          else
            echo "apiDomain=$API_VERSION_URI" >> $GITHUB_OUTPUT
          fi

      - name: Display VERSION_URI
        run: echo ${{ steps.getGAEVersionURI.outputs.result }}
