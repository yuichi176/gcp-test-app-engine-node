name: DELETE_PREVIEW_VERSION

on:
  workflow_dispatch:
  pull_request:
    types: [closed]
    branches:
      - develop

jobs:
  delete:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - id: auth
        name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: Create Version Name from Branch Name
        id: create_version_name
        uses: actions/github-script@v6
        env:
          BRANCH_NAME: ${{ github.head_ref }}
        with:
          result-encoding: string
          script: |
            try {
              const versionName = process.env.BRANCH_NAME.replaceAll('/', '-')
              return versionName
            } catch (error) {
              core.error("Failed to create version name")
              core.setFailed(error)
            }

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Delete Preview Version
        run: gcloud app services delete --quiet --project=gcp-test-420611 --version=${{ steps.create_version_name.outputs.result }}
